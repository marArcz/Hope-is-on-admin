export type MenuItem = {
    to: string;
    icon: string;
    label: string;
};

export type PetModel = {
    image: string;
  name: string;
  description: string;
  age: string;
  address: string;
  gender: string;
    
}